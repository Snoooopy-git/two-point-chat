# Changelog

本项目遵循语义化版本。未发布变更记录在 `Unreleased`。

## Unreleased

## 0.4.1 - 2026-09-24

- 在应用启动、HTTP 请求和 Socket 连接中统一处理过期会话，避免失效 token 被误显示为好友清空。
- 好友列表增加加载、失败和真实空状态，并提供失败重试。
- 增强部署数据库门禁，校验好友申请表、外键完整性和双向好友关系对称性。
- 增加过期会话与数据库验证回归测试。

## 0.4.0 - 2026-09-20

- 增加自适应右侧工具栏：桌面三栏、中屏侧滑面板、移动端全屏工具页。
- 增加本地月历，支持月份切换、返回今天和日期选择。
- 接入原创“跳跳信使”桌面宠物，支持待机、指针响应、点击、拖动、散步、休眠及聊天状态联动。

## 0.3.5 - 2026-09-18

- 添加需对方确认的好友申请流程，支持发送、实时提醒、通过、拒绝和取消。
- 将常驻好友搜索框改为带待处理角标的加号入口，并提供桌面弹窗和移动端底部面板。
- 将联系人最后消息查询合并为单条 SQL，消除好友列表 N+1 查询。
- 部署阶段改用 lockfile 精确重装生产依赖，避免 `npm prune` 造成依赖版本漂移。
- 修复已读回执投递目标错误：标记已读时改为通知原发送方，并在消息气泡显示已读/未读。
- 修复 `PUT /api/messages/:friendId/read` 缺少好友 ID 校验，非法输入改为返回 400。
- 修复用户搜索未转义 LIKE 通配符、且未过滤已禁用账号的问题。
- 界面版本号改为从 `client/package.json` 读取，消除硬编码版本漂移。
- 移除未使用的 `isUserOnline` 导出。

## 0.3.4 - 2026-07-27

- 将 SQLite UTC 消息时间序列化为带时区的 ISO 格式，修复聊天气泡时间固定偏差。
- 增加历史消息、Socket 消息和东八区显示回归验证。

## 0.3.3 - 2026-07-27

- 将客户端 PostCSS 约束在已修复 `GHSA-r28c-9q8g-f849` 的版本范围，并增加依赖回归验证。
- 禁止生产依赖裁剪写入 lockfile，并在 PM2 更新前校验根目录与客户端 lockfile 指纹。

## 0.3.0 - 2026-07-24

- 统一 HTTP 与 Socket 的账号状态校验，并在禁用或删除账号时撤销在线会话。
- 上传改为真实图片签名验证、安全文件名和可外置数据目录。
- 修复管理员保护、好友双向事务、软删除消息摘要和未知 API 挂起问题。
- 消息历史改为最近页游标分页，并增加客户端消息 ID 幂等确认。
- 重构客户端 Socket 生命周期、跨账号状态清理、在线快照和已读持久化。
- 增加认证、管理员、好友、分页、上传和双客户端 Socket 集成测试。

## 0.2.0 - 2026-07-24

- 重构开发治理规则并引入风险分级。
- 增加架构、测试、安全、部署和事故复盘文档。
- 增加静态检查、自动化测试、CI、安全扫描和治理检查。
- 移除硬编码生产密钥及可抢注的管理员引导方式。
- 增加 SQLite 在线备份、完整性验证和恢复演练工具。
- 加固部署脚本的备份、构建、原生模块重建和健康检查流程。

## 0.1.2 - 2026-07-21

- 修复 emoji picker 的网络、Web Component、布局和事件集成问题。
- 增加管理员后台及消息软删除字段。
