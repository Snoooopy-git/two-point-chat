# Development example only. Production secrets must come from the runtime environment.
NODE_ENV=development
PORT=3000
CHAT_DB_PATH=server/chat.db
CHAT_UPLOAD_PATH=server/uploads
JWT_SECRET=replace-with-a-random-value-of-at-least-32-characters
