# 双点聊天开发治理

本文件是本仓库的强制执行入口。项目事实、操作手册和事故复盘分别维护在 `docs/` 下，避免治理规则与易变信息混杂。

## 一、优先级与基本原则

1. 用户明确要求高于仓库默认流程；安全、数据保护和权限边界不得被隐式放宽。
2. 结论优先，证据紧随其后。不得把猜测写成事实。
3. 调研先于方案，方案先于编码。高风险假设未验证时禁止编码。
4. 只修改已确认范围。发现范围外问题时记录并报告，不得顺手修改。
5. 开发、发布、部署严格分离。未收到明确上线指令时，禁止任何远程或生产操作。
6. 用户现有改动属于用户。禁止覆盖、回滚、暂存或提交无关改动。

## 二、风险分级与确认门槛

| 等级 | 典型操作 | 执行规则 |
|---|---|---|
| L0 只读 | 阅读、搜索、审阅、查看状态 | 可直接执行 |
| L1 本地可逆 | 已明确的小范围编辑、格式化、构建、测试、临时目录验证 | 先声明范围，可直接执行 |
| L2 重要状态变更 | 数据迁移、删除、版本号、依赖、commit、tag | 必须明确确认目标与范围 |
| L3 外部/生产 | push、PR、SSH、SCP、部署、重启、生产数据操作 | 必须明确确认目标环境、版本、验证和回退 |

- 需求明确且属于 L0/L1：输出精确范围后执行，无需重复确认。
- 需求模糊、存在架构冲突或属于 L2/L3：先澄清并等待确认。
- 只有存在实质性的架构、成本、体验或兼容性取舍时，才必须提供 A/B/C 三个方案。
- 执行中需要扩大或缩小已确认范围时必须停止，输出差异并重新确认。

## 三、分析与变更纪律

实施前必须记录：

- 目标与非目标；
- 影响面；
- 已验证、待验证和无法验证的假设；
- 主路径、异常路径；
- 高风险变更的回退策略。

禁止：

- 在应用配置或通用脚本中写入机器专属绝对路径；
- 硬编码生产密钥、令牌、账号或私钥；
- 引入未使用的依赖、导入、变量或调试代码；
- 用 `git add -A`、`git add .` 默认暂存整个工作区；
- 未经确认修改版本号、创建 commit/tag、push 或部署；
- 以吞掉异常、关闭检查或降低规则强度的方式让验证通过。

## 四、技术约束

- Express 5 通配符路由使用 `/{*path}`，兜底处理器必须响应或调用 `next()`。
- 生产 Express 绑定 `127.0.0.1:3000`，由 Nginx 代理。
- SQLite 使用 WAL、单进程 PM2 fork 模式；数据库和上传文件属于环境数据，不属于代码。
- `better-sqlite3` 必须在目标 Linux 环境执行 `npm rebuild better-sqlite3`。
- 生产环境必须显式提供高强度 `JWT_SECRET`，缺失或弱密钥时服务必须拒绝启动。
- Vue Web Component 必须配置 `isCustomElement`，使用 Vue 模板事件绑定，并在首次挂载时处于可见区域。
- 详细架构约束见 `docs/architecture.md`。

## 五、质量门禁与交付定义

每次代码交付必须：

1. 范围内事项全部完成，范围外无改动。
2. 运行与影响面匹配的最小验证；安全、认证、数据或部署变更必须运行完整门禁。
3. `npm run check` 通过；若环境限制导致某项无法运行，必须报告命令、原因和剩余风险。
4. 无新增错误、警告、临时文件、调试代码和未使用依赖。
5. 检查 `git diff --check`、`git diff` 和 `git status --short`。
6. 报告实际变更、执行命令、结果、未验证项和剩余风险。

测试矩阵与证据要求见 `docs/testing.md`。

## 六、Bug 修复纪律

1. 先复现并收集证据，再列出独立根因和候选假设。
2. 涉及第三方组件时创建最小可复现环境；形式可为 HTML、Node 脚本、单元测试、内存数据库或组件测试。
3. 修复已确认根因，并为每个根因增加对应回归验证。
4. 同一根因连续修复两次仍未通过时停止，报告复现路径、已排除假设、剩余可能性和下一步验证方法。
5. 修复后必须按用户原始路径执行端到端验证，不能只验证改动点。
6. 隔离文件优先放系统临时目录；只有具备长期回归价值时才加入测试目录。

## 七、安全与数据库纪律

- 认证、权限、上传、敏感配置和外部输入必须遵循 `docs/security.md`。
- 数据库迁移必须可识别、可失败、可验证；禁止把所有异常当成“已迁移”。
- 生产数据库变更前必须创建一致性备份，并执行恢复演练或验证最近一次恢复演练仍有效。
- 禁止将 `chat.db*`、上传文件、日志、备份或生产 `.env` 纳入发布包或 Git。
- 数据库备份必须位于代码目录之外，并通过 `PRAGMA integrity_check`。

## 八、版本、提交与发布

- 工作项、Git 提交和正式发布是不同概念；只有正式发布才修改语义化版本。
- 版本号变更必须明确确认，并同步更新根目录与客户端 `package.json` 及 lockfile。
- 创建 commit/tag 前必须检查暂存内容，仅显式暂存确认范围内文件。
- 每个正式版本对应一个 commit 和一个 `vMAJOR.MINOR.PATCH` tag。
- 禁止跳过版本、倒退版本或重写已发布 tag。
- push、PR 和部署始终属于独立的 L3 授权，不因允许 commit/tag 而自动获得授权。

## 九、部署边界

- 未收到明确上线指令时，禁止执行 `git push`、SSH、SCP、rsync、远程命令、PM2/Nginx 操作或 `deploy.sh`。
- 上线必须指定版本/tag、目标环境、数据库备份位置、验证清单和回退基线。
- 部署前后必须验证数据库完整性、健康检查、前端加载和关键 API。
- 失败时立即停止；只有预先确认且不涉及数据回滚的自动回退才可直接执行。
- 完整流程见 `docs/deployment.md`，禁止使用文档之外的快捷部署命令。

## 十、文档索引

- 架构与环境：`docs/architecture.md`
- 测试与交付证据：`docs/testing.md`
- 安全基线：`docs/security.md`
- 部署与回退：`docs/deployment.md`
- 事故复盘：`docs/incidents/`
